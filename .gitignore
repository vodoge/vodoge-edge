/target/
*.rs.bk
.DS_Store

# trunk 的产物。**刻意不提交**：`docs/frontend-rebuild/README.md` 定的是
# 「边缘端的 WASM 在边缘机上构建，不提交产物进仓库，仓库保持没有二进制」。
#
# ⚠️ 代价是构建有顺序：`edge-panel` 用 include_bytes! 把这些文件嵌进二进制，
# 所以必须先 `cd edge-ui && trunk build --release --public-url /next/`，
# 再 `cargo build -p edge-panel`。缺了 dist 时 cargo 会报 include_bytes! 找不到
# 文件——错误信息本身不会告诉你该跑 trunk，所以记在这里。
/edge-ui/dist/
